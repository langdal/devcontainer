# Debian 13 (trixie) cloud image — stable, no AppArmor by default.
# Immutable dated build (the moving trixie/latest/ URL gets re-published
# every few weeks, which breaks the sha pin). To bump: pick the newest
# dated dir at https://cloud.debian.org/images/cloud/trixie/ and verify
# against its SHA512SUMS (Debian publishes no SHA256SUMS; compute the
# sha256 from the downloaded file after the sha512 checks out).
IMAGE_URL="https://cloud.debian.org/images/cloud/trixie/20260623-2518/debian-13-genericcloud-amd64-20260623-2518.qcow2"
IMAGE_SHA256="e32d03ec8f05b0b3a4c16e8cc0a5424efa78514141a60a2656623cf422cc6416"
CLOUD_USER="debian"
PACKAGES="docker.io docker-cli docker-buildx podman jq rsync"
PACKAGE_INSTALL_CMD="apt-get install -y --no-install-recommends"
POST_BOOT_CMDS=""
